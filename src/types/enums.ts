export enum Language {
  Ruski = 'russian' 
}

export enum LanguageIso {
  English = 'en',
  Russia = 'ru',
  Swedish = 'sv',
  Danish = 'da',
  Norwegian = 'no'
}

export enum CommandArg {
  Language = '--lang',
  Port = '--port',
  Host = '--host'
}